<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ninimum.api.tariff.service.TariffMapper">

    <select id="getTariffList"
            resultType="com.ninimum.api.dto.TariffDto">
        SELECT
            id AS tariffId,
            name AS tariffName,
            CAST(monthly_price AS SIGNED) AS price,
            duration_months AS durationMonth,
            description AS description
        FROM tariff_plans
        WHERE is_active = 1
        ORDER BY monthly_price ASC, id ASC
    </select>

    <select id="getTariffDetail"
            parameterType="com.ninimum.api.param.TariffDetailParam"
            resultType="com.ninimum.api.dto.TariffDto">
        SELECT
            id AS tariffId,
            name AS tariffName,
            CAST(monthly_price AS SIGNED) AS price,
            duration_months AS durationMonth,
            description AS description
        FROM tariff_plans
        WHERE id = #{tariffId}
          AND is_active = 1
        LIMIT 1
    </select>

</mapper>
